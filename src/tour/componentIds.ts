export const buildFileTourId = (fileName: string) => `file-${fileName}`;

export const buildFolderTourId = (folderName: string) => `folder-${folderName}`;

export const STATIC_TOUR_IDS = {
  CODE_EDITOR: 'codeEditor'
};
